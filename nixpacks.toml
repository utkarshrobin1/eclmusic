# Railway uses Nixpacks to build your app.
# This installs FFmpeg and other system packages automatically.

[phases.setup]
nixPkgs = ["ffmpeg", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python bot.py"
